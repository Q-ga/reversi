# CLAUDE.md — リバーシ

## Agent skills

### Issue tracker

Issues・PRDは GitHub Issues（Q-ga/reversi、`gh` CLI）で管理する。詳細は `docs/agents/issue-tracker.md`。

### Triage labels

5ロールとも既定文字列（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。対応表は `docs/agents/triage-labels.md`。

### Domain docs

単一コンテキスト構成——ルートの `CONTEXT.md`（価値定義＋用語集）と `docs/adr/`。読み方の規約は `docs/agents/domain.md`。
